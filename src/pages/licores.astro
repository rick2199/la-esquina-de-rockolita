---
// Página de licores con subcategorías
import catalogData from "../data/catalog.json"
import "../styles/globals.css"

const category = catalogData.licores
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="description"
      content="Licores - La Esquina de Rockolita. Whisky, Ron, Vodka, Pisco, Gin, Vinos y más"
    />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Licores - La Esquina de Rockolita</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.png" />
  </head>

  <body class="font-brand bg-rockolita-cream">
    <!-- Header -->
    <header class="rockolita-header relative overflow-hidden">
        <!-- Efecto de fondo sutil -->
        <div class="absolute inset-0 bg-gradient-to-br from-white via-rockolita-cream/30 to-rockolita-cream/50"></div>
        
        <div class="container mx-auto px-4 py-8 relative">
          <div class="text-center">
            <a href="/" class="group hover:opacity-90 transition-all duration-300">
              
              <!-- Logo principal con animaciones mejoradas -->
              <div class="flex items-center justify-center gap-4 mb-4">
                <!-- Perro izquierdo -->
                <div class="text-4xl md:text-5xl rockolita-wiggle transform group-hover:scale-110 transition-transform duration-300">
                  🐕
                </div>
                
                <!-- Logo central con efecto -->
                <div class="relative">
                  <!-- Sombra suave detrás del logo -->
                  <div class="absolute inset-0 bg-rockolita-primary/20 rounded-full blur-xl transform scale-110"></div>
                  
                  <!-- Logo principal -->
                  <img
                    src="https://d2ns2vv9idwaki.cloudfront.net/licoreria/logo-la-esquina-de-rockolita.png"
                    alt="La Esquina de Rockolita"
                    class="relative w-36 md:w-40 h-auto mx-auto transform group-hover:scale-105 transition-transform duration-300 filter drop-shadow-lg"
                    loading="lazy"
                  />
                </div>
                
                <!-- Cerveza derecha -->
                <div class="text-4xl md:text-5xl rockolita-wiggle transform group-hover:scale-110 transition-transform duration-300">
                  🍺
                </div>
              </div>
  
              <!-- Subtitle con mejor espaciado -->
              <div class="flex items-center justify-center gap-3 mb-2">
                <div class="w-8 md:w-12 h-0.5 bg-gradient-to-r from-transparent via-rockolita-primary to-rockolita-primary rounded-full"></div>
                <p class="brand-subtitle text-rockolita-secondary text-sm md:text-base uppercase tracking-wider font-medium">
                  Catálogo Digital
                </p>
                <div class="w-8 md:w-12 h-0.5 bg-gradient-to-l from-transparent via-rockolita-primary to-rockolita-primary rounded-full"></div>
              </div>
  
              <!-- Tagline adicional -->
              <p class="brand-text text-rockolita-secondary/80 text-xs mt-2 font-light">
                Tu esquina de confianza
              </p>
              
            </a>
          </div>
        </div>
  
        <!-- Línea decorativa inferior -->
        <div class="h-1 bg-gradient-to-r from-rockolita-primary/30 via-rockolita-primary to-rockolita-primary/30"></div>
    </header>

    <!-- Contenido principal -->
    <main class="container mx-auto px-4 py-8">
      <div class="max-w-6xl mx-auto">
        <!-- Breadcrumb -->
        <nav class="flex items-center gap-2 text-sm text-rockolita-secondary mb-8">
          <a href="/" class="hover:text-rockolita-primary transition-colors font-medium">🏠 Inicio</a>
          <span class="text-rockolita-primary">→</span>
          <span class="text-rockolita-dark font-semibold">{category.title}</span>
        </nav>

        <!-- Header de categoría -->
        <div class="text-center mb-12">
          <div class="text-6xl md:text-8xl mb-6 rockolita-wiggle">{category.icon}</div>
          <h1 class="brand-title text-3xl md:text-4xl text-rockolita-dark mb-4">
            {category.title}
          </h1>
          <p class="brand-text text-rockolita-secondary text-lg max-w-2xl mx-auto">
            {category.description}
          </p>
          <div class="golden-line mt-8">
            <div class="text-xl">🥃</div>
          </div>
        </div>

        <!-- Grid de subcategorías -->
        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
          {
            Object.entries(category.subcategories).map(
              ([subKey, subcategory]) => (
                <button
                  onclick={`openProductDialog('${subKey}', '${subcategory.title}', '${subcategory.description}', ${JSON.stringify(subcategory.products)})`}
                  class="rockolita-card group p-6 text-center transition-all duration-300"
                >
                  <div class="text-3xl md:text-4xl mb-4 group-hover:animate-wiggle transition-transform duration-300">
                    {subcategory.icon}
                  </div>
                  <h3 class="brand-subtitle font-bold text-rockolita-dark group-hover:text-rockolita-primary transition-colors mb-2">
                    {subcategory.title}
                  </h3>
                  <p class="brand-text text-xs text-rockolita-secondary mb-3">
                    {subcategory.description}
                  </p>
                  <div class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-rockolita-primary/20 text-rockolita-secondary border border-rockolita-primary/30">
                    {subcategory.products.length} productos
                  </div>
                </button>
              )
            )
          }
        </div>

        <!-- Información especial sobre licores -->
        <div class="mt-12 warning-box p-6 max-w-4xl mx-auto">
          <div class="flex items-start gap-4">
            <div class="text-3xl">⚠️</div>
            <div>
              <h3 class="brand-subtitle font-bold text-rockolita-red mb-2">Consumo Responsable</h3>
              <p class="brand-text text-rockolita-dark text-sm leading-relaxed">
                <strong>EL CONSUMO EXCESIVO DE ALCOHOL ES DAÑINO</strong> • 
                La venta está prohibida a menores de 18 años • 
                Todos nuestros licores son 100% originales y de calidad garantizada
              </p>
            </div>
          </div>
        </div>

        <!-- Botón volver -->
        <div class="mt-12 text-center">
          <a
            href="/"
            class="rockolita-btn inline-flex items-center gap-2 px-6 py-3 rounded-lg font-medium transition-all duration-300"
          >
            <svg
              class="w-4 h-4"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
            </svg>
            Volver al inicio
          </a>
        </div>
      </div>
    </main>

    <!-- Dialog de productos con nueva identidad -->
    <div
      id="product-dialog"
      class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4 z-50 hidden"
    >
      <div
        class="bg-white rounded-lg max-w-6xl w-full max-h-[95vh] overflow-hidden flex flex-col border-4 border-rockolita-primary"
      >
        <!-- Header del dialog -->
        <div class="flex items-center justify-between p-6 border-b-2 border-rockolita-cream rockolita-gradient">
          <div>
            <h2 id="dialog-title" class="brand-title text-2xl font-bold text-white"></h2>
            <p id="dialog-description" class="brand-text text-white/90 text-sm mt-1"></p>
          </div>
          <button
            id="close-dialog"
            class="p-2 hover:bg-white/20 rounded-full transition-colors text-white"
            aria-label="Cerrar"
          >
            <svg
              class="w-6 h-6"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>

        <!-- Panel de filtros con estilo de marca -->
        <div class="border-b bg-rockolita-cream/50 p-4">
          <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
            <!-- Búsqueda -->
            <div class="md:col-span-2">
              <div class="relative">
                <svg
                  class="absolute left-3 top-1/2 transform -translate-y-1/2 text-rockolita-secondary w-4 h-4"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                </svg>
                <input
                  type="text"
                  id="search-input"
                  placeholder="Buscar productos..."
                  class="w-full pl-10 pr-4 py-2 border-2 border-rockolita-primary/30 rounded-md text-sm focus:ring-2 focus:ring-rockolita-primary focus:border-rockolita-primary bg-white"
                />
              </div>
            </div>

            <!-- Filtro por marca -->
            <select
              id="brand-filter"
              class="px-3 py-2 border-2 border-rockolita-primary/30 rounded-md text-sm focus:ring-2 focus:ring-rockolita-primary focus:border-rockolita-primary bg-white"
            >
              <option value="">Todas las marcas</option>
            </select>

            <!-- Filtro por precio -->
            <select
              id="price-filter"
              class="px-3 py-2 border-2 border-rockolita-primary/30 rounded-md text-sm focus:ring-2 focus:ring-rockolita-primary focus:border-rockolita-primary bg-white"
            >
              <option value="">Precio máximo</option>
              <option value="30">Hasta S/. 30</option>
              <option value="50">Hasta S/. 50</option>
              <option value="100">Hasta S/. 100</option>
              <option value="200">Hasta S/. 200</option>
            </select>
          </div>

          <!-- Botón limpiar filtros -->
          <div class="mt-4 flex justify-end">
            <button
              id="clear-filters"
              class="rockolita-btn-secondary px-4 py-2 rounded-md text-sm font-medium transition-all duration-300"
            >
              Limpiar filtros
            </button>
          </div>
        </div>

        <!-- Grid de productos -->
        <div class="flex-1 overflow-y-auto p-6 bg-rockolita-cream/20">
          <div
            id="products-grid"
            class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-4"
          >
            <!-- Los productos se cargan dinámicamente -->
          </div>

          <!-- Mensaje cuando no hay productos después de filtrar -->
          <div id="no-products" class="text-center py-12 hidden">
            <div class="text-6xl mb-4">🔍</div>
            <p class="brand-subtitle text-rockolita-secondary text-lg mb-2">
              No se encontraron productos
            </p>
            <p class="brand-text text-rockolita-secondary/70 text-sm">
              Intenta ajustar los filtros de búsqueda
            </p>
          </div>

          <!-- Mensaje cuando la categoría está vacía -->
          <div id="empty-category" class="text-center py-12 hidden">
            <div class="text-6xl mb-4">📦</div>
            <p class="brand-subtitle text-rockolita-secondary text-lg mb-2">
              No hay productos disponibles
            </p>
            <p class="brand-text text-rockolita-secondary/70 text-sm">
              Próximamente añadiremos más productos a esta categoría
            </p>
          </div>
        </div>

        <!-- Footer con contador -->
        <div class="border-t-2 border-rockolita-cream rockolita-gradient px-6 py-3">
          <p id="results-count" class="brand-text text-sm text-white font-medium">
            Mostrando 0 productos
          </p>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <footer class="rockolita-footer py-6 mt-16">
      <div class="container mx-auto px-4 text-center">
        <div class="flex items-center justify-center gap-2 mb-4">
          <div class="text-2xl">🐕</div>
          <img
            src="https://d2ns2vv9idwaki.cloudfront.net/licoreria/logo-la-esquina-de-rockolita.png"
            alt="La Esquina de Rockolita"
            class="w-24 h-auto"
            loading="lazy"
          />
          <div class="text-2xl">🍷</div>
        </div>
        <div class="brand-text text-xs text-rockolita-cream/80">
          <p>📍 Calle Camino Real 181, El Agustino - Lima 📞 913 930 329</p>
          <p class="mt-1">🕒 Horarios de atención: Lunes a Domingo 24 horas</p>
          <p class="mt-2 text-rockolita-primary font-medium">Tu esquina de confianza 🏪</p>
        </div>
      </div>
    </footer>

    <script>
      let currentProducts: Array<any> = []
      let hasActiveFilters: boolean = false

      // Función para abrir el dialog de productos
      function openProductDialog(
        subcategoryKey: string,
        title: string,
        description: string,
        products: Array<any>
      ) {
        currentProducts = products
        hasActiveFilters = false
        
        const dialog: HTMLElement | null =
          document.getElementById("product-dialog")
        const dialogTitle: HTMLElement | null =
          document.getElementById("dialog-title")
        const dialogDescription: HTMLElement | null =
          document.getElementById("dialog-description")
        const productsGrid: HTMLElement | null =
          document.getElementById("products-grid")
        const emptyCategory: HTMLElement | null =
          document.getElementById("empty-category")

        if (dialogTitle) dialogTitle.textContent = title
        if (dialogDescription) dialogDescription.textContent = description

        // Limpiar filtros
        clearAllFilters()

        if (products.length === 0) {
          if (productsGrid) productsGrid.innerHTML = ""
          if (emptyCategory) emptyCategory.classList.remove("hidden")
        } else {
          if (emptyCategory) emptyCategory.classList.add("hidden")
          renderProducts(products)
          generateBrandFilter(products)
        }

        if (dialog) {
          dialog.classList.remove("hidden")
          document.body.style.overflow = "hidden"
          
          const noProductsMsg: HTMLElement | null = document.getElementById("no-products")
          if (noProductsMsg) noProductsMsg.classList.add("hidden")
        }

        updateResultsCount()
      }

      // Renderizar productos con estilos de marca
      function renderProducts(products: Array<any>) {
        const productsGrid: HTMLElement | null =
          document.getElementById("products-grid")

        if (productsGrid) {
          productsGrid.innerHTML = products
            .map((product: any, index: number) => {
              const minPrice: number = Math.min(
                ...product.presentations.map((p) => parseFloat(p.price))
              )

              return `
            <div 
            class="product-card bg-white rounded-lg border-2 border-rockolita-cream shadow-sm hover:shadow-rockolita transition-all duration-200 hover:border-rockolita-primary"
            data-name="${product.name.toLowerCase()}"
            data-brand="${product.brand.toLowerCase()}"
            data-min-price="${minPrice}"
            data-presentations="${product.presentations.map((p) => p.type.toLowerCase()).join(",")}"
            >
            <div class="product-image-container aspect-[3/4] p-4 flex items-center justify-center bg-gradient-to-br from-white to-rockolita-cream/30 rounded-t-lg">
                <img 
                src="${product.img}" 
                alt="${product.name}"
                class="max-w-full max-h-full object-contain hover:scale-105 transition-transform duration-200"
                loading="lazy"
                />
            </div>
            
            <div class="product-content p-4">
                <h3 class="product-title font-semibold text-rockolita-dark mb-3 text-sm leading-tight">
                <span class="line-clamp-2">${product.name}</span>
                </h3>
                
                <div class="product-presentations">
                <div class="space-y-2">
                    ${product.presentations
                    .map(
                        (presentation) => `
                    <div class="presentation-item flex items-center justify-between p-2 rounded-md bg-rockolita-cream/50 border border-rockolita-primary/20 hover:bg-rockolita-primary/10 hover:border-rockolita-primary/40 transition-all duration-200 min-h-[2.5rem]">
                        <span class="text-xs text-rockolita-secondary font-medium flex-1 pr-2 truncate" title="${presentation.type}">
                        ${presentation.type}
                        </span>
                        <span class="presentation-price text-xs font-bold text-rockolita-secondary whitespace-nowrap">
                        S/. ${presentation.price}
                        </span>
                    </div>
                    `
                    )
                    .join("")}
                </div>
                
                ${
                    product.presentations.length > 1
                    ? `
                    <div class="mt-3 pt-2 border-t border-rockolita-cream">
                    <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-rockolita-primary/20 text-rockolita-secondary border border-rockolita-primary/30">
                        ${product.presentations.length} presentaciones
                    </span>
                    </div>
                `
                    : ""
                }
                </div>
            </div>
            </div>
          `
            })
            .join("")
        }
      }

      // Resto de las funciones JavaScript mantienen la misma lógica...
      function generateBrandFilter(products: Array<any>) {
        const brandFilter: HTMLElement | null =
          document.getElementById("brand-filter")
        const brands: Array<string> = [
          ...new Set(products.map((p) => p.brand)),
        ].sort()

        if (brandFilter) {
          brandFilter.innerHTML =
            '<option value="">Todas las marcas</option>' +
            brands
              .map(
                (brand) =>
                  `<option value="${brand.toLowerCase()}">${brand}</option>`
              )
              .join("")
        }
      }

      function applyFilters() {
        const searchTerm: string = (
          document.getElementById("search-input") as HTMLInputElement
        ).value.toLowerCase()
        const selectedBrand: string = (
          document.getElementById("brand-filter") as HTMLInputElement
        ).value
        const maxPrice: number | null = document.getElementById("price-filter")
          ? parseFloat(
              (document.getElementById("price-filter") as HTMLInputElement)
                .value
            )
          : null

        hasActiveFilters = searchTerm !== "" || selectedBrand !== "" || maxPrice !== null

        const productCards: NodeListOf<HTMLElement> =
          document.querySelectorAll(".product-card")
        let visibleCount: number = 0

        productCards.forEach((card) => {
          let isVisible: boolean = true

          if (
            searchTerm &&
            card.dataset.name &&
            !card.dataset.name.includes(searchTerm)
          ) {
            isVisible = false
          }

          if (selectedBrand && card.dataset.brand !== selectedBrand) {
            isVisible = false
          }

          if (
            maxPrice !== null &&
            card.dataset.minPrice &&
            parseFloat(card.dataset.minPrice) > maxPrice
          ) {
            isVisible = false
          }

          if (isVisible) {
            card.style.display = "block"
            visibleCount++
          } else {
            card.style.display = "none"
          }
        })

        updateResultsCount(visibleCount)

        const noProductsMsg: HTMLElement | null =
          document.getElementById("no-products")
        if (noProductsMsg) noProductsMsg.classList.add("hidden")
        
        if (hasActiveFilters && visibleCount === 0 && currentProducts.length > 0) {
          if (noProductsMsg) noProductsMsg.classList.remove("hidden")
        }
      }

      function updateResultsCount(visibleCount: number | null = null) {
        const resultsCount: HTMLElement | null =
          document.getElementById("results-count")
        const count: number =
          visibleCount !== null ? visibleCount : currentProducts.length
        if (resultsCount)
          resultsCount.textContent = `Mostrando ${count} de ${currentProducts.length} productos`
      }

      function clearAllFilters() {
        ;(document.getElementById("search-input") as HTMLInputElement).value = ""
        ;(document.getElementById("brand-filter") as HTMLInputElement).value = ""
        ;(document.getElementById("price-filter") as HTMLInputElement).value = ""

        hasActiveFilters = false
        applyFilters()
      }

      // Event listeners
      document.addEventListener("DOMContentLoaded", function () {
        const dialog: HTMLElement | null =
          document.getElementById("product-dialog")
        const closeBtn: HTMLElement | null =
          document.getElementById("close-dialog")
        const searchInput: HTMLElement | null =
          document.getElementById("search-input")
        const brandFilter: HTMLElement | null =
          document.getElementById("brand-filter")
        const priceFilter: HTMLElement | null =
          document.getElementById("price-filter")
        const clearFiltersBtn: HTMLElement | null =
          document.getElementById("clear-filters")

        if (closeBtn)
          closeBtn.addEventListener("click", () => {
            if (dialog) {
              dialog.classList.add("hidden")
              document.body.style.overflow = ""
            }
          })

        if (dialog)
          dialog.addEventListener("click", (e) => {
            if (e.target === dialog) {
              dialog.classList.add("hidden")
              document.body.style.overflow = ""
            }
          })

        if (searchInput) searchInput.addEventListener("input", applyFilters)
        if (brandFilter) brandFilter.addEventListener("change", applyFilters)
        if (priceFilter) priceFilter.addEventListener("change", applyFilters)
        if (clearFiltersBtn)
          clearFiltersBtn.addEventListener("click", clearAllFilters)

        document.addEventListener("keydown", (e) => {
          if (e.key === "Escape" && !dialog?.classList.contains("hidden")) {
            if (dialog) {
              dialog.classList.add("hidden")
              document.body.style.overflow = ""
            }
          }
        })
      })

      ;(window as any).openProductDialog = openProductDialog
    </script>
  </body>
</html>